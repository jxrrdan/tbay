# Install the adapter you need. Both are kept here for convenience.
# DuckDB (local, zero-config):
dbt-duckdb>=1.10.1,<2.0
dbt-core>=1.11.12,<2.0

# BigQuery (uncomment to use instead):
# dbt-core>=1.11.12,<2.0
# dbt-bigquery>=1.7,<2.0
